#include <stdio.h>

 int main ()
 {
 	
 	/*
 	int umut=10;
 	int *ptr;
 	
 	ptr=&umut;
 	
	 printf("%d\n",*ptr); //içeriðini gösterecek
	 printf("%d\n", ptr); // adresini gösterecek
	 
	 printf("%d\n", umut); //içeriðini gösterecek
	 printf("%d\n", &umut); // adresini gösterecek
*/
	 	int *sayi;
	 	int n;
	 	n=20;
	 	
	 	sayi=&n;
	 	2
	 printf("%d\n", *sayi); //içeriðini gösterecek
	 printf("%d\n", sayi); // adresini gösterecek
	 
	 printf("%d\n", n); //içeriðini gösterecek
	 printf("%d\n", &n); // adresini gösterecek
	 	
	 	*sayi=30;
	 	
	 	printf("%d\n",n); //içeriðini gösterecek
	 	printf("%d\n", &n);// adresini gösterecek
	 	
	 	
 	
 	return 0; 
 }
